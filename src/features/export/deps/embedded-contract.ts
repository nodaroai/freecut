/**
 * Adapter exports for embedded-mode (studio iframe) dependencies.
 * Export modules should import the studio hand-off from here.
 */

export { sendExportToStudio } from '@/features/embedded/services/studio-export-bridge'
